- 2026-03-23: Checked Windows↔VPS↔mac SSH bridge. Windows sshd on port 1022 is healthy; reverse tunnel can establish but connections via VPS were being reset. Updated Windows tunnel script to use system OpenSSH client and explicit IPv4 target 127.0.0.1:1022 for stability.
- 2026-03-23: Repair succeeded after switching the Windows reverse tunnel from a non-system ssh.exe + `localhost:1022` target to `C:\Windows\System32\OpenSSH\ssh.exe` + `127.0.0.1:1022`. VPS resumed listening on port 42022, and the user confirmed MacBook -> Windows access recovered.
- 2026-03-23: Configured Windows boot auto-start for the SSH reverse tunnel. Added scheduled task `OpenClaw-Windows-Bridge-Boot` (AtStartup, runs as SYSTEM), created `C:\ProgramData\ssh\openclaw_reverse_tunnel_boot.ps1`, and stored a restricted-permission copy of the bridge key at `C:\ProgramData\ssh\openclaw_bridge_key` so macOS can reconnect after Windows reboots without waiting for an interactive login.
